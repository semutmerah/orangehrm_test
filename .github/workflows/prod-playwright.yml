name: Production Playwright Tests
on:
  # push:
  #   branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    environment: production
    strategy:
      fail-fast: false
      matrix:
        shardIndex: [1, 2]
        shardTotal: [2]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-node@v6
      with:
        node-version: lts/*
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci

    - name: Install Playwright Browsers
      run: npx playwright install --with-deps

    - name: Run Playwright tests
      run: npx playwright test --shard=${{ matrix.shardIndex }}/${{ matrix.shardTotal }}
      env:
        ENV: production
        BASE_URL: ${{ secrets.BASE_URL }}
        USERNAME: ${{ secrets.USERNAME }}
        PASSWORD: ${{ secrets.PASSWORD }}

    - name: Upload Report Shard
      uses: actions/upload-artifact@v6
      if: ${{ !cancelled() }}
      with:
        name: playwright-report-production-${{ matrix.shardIndex }}
        path: playwright-report/
        retention-days: 1

  merge-reports:
    if: ${{ !cancelled() }}
    needs: [test]
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-node@v6
      with:
        node-version: lts/*
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci

    - name: Download all shards
      uses: actions/download-artifact@v7
      with:
        path: all-shards
        pattern: playwright-report-production-*
        merge-multiple: true

    - name: Merge reports
      run: npx playwright merge-reports --reporter html ./all-shards

    - name: Upload consolidated report
      uses: actions/upload-artifact@v6
      with:
        name: playwright-report-production-final
        path: playwright-report/
        retention-days: 1
